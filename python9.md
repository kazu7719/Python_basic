## コメントを書く場合

print("Hello World")     # 実行される

# print("Hello World")  # 実行されない

※全角スペースが冒頭にあるとエラーが起きる

## len観数
・・・文字列の文字数を数える観数

len("Hello World")

## str観数
・・・数値が持つ観数。数値を文字列に変換してくれる
str = string(文字列)

str(20)

## int観数
・・・文字列が持つ観数。文字列を数値に変換してくれる
int = integer(整数)

int("30")

## f文字列

f"文字列{式}文字列"

f"今日で{19 + 1}歳になりました"


## print観数
・・・rubyでいうputsと同様

name = "Taro"

print(name)

## input観数
・・・rubyでいうgetsと同様

name = input()

print(name)
