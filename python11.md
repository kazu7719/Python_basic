### 条件処理

## if文
if 条件式:
    処理

→処理の前にインデントを半角スペース4つ分入れる

## else(if文)
if 条件式:
    処理
else:
    処理

## elif(if文)
if 条件式1:
    処理
elif 条件式2:
    処理
else:
    処理

### 繰り返し処理

## for文

for 変数 in 配列名:
    実行する処理

## range観数

for 変数 in range(繰り返したい回数):
    実行する処理

for i in range(10):
    print(i + 1)

### 関数の定義

def 関数名():
    実行する処理(インデントが必要)

## return文(戻り値)

def sample():
    num = 2 * 3
    return num

print(sample())
# => 6

## グローバルスコープ
・・・関数の外側で変数を定義すること
範囲はファイル内全体で、その中で定義した変数をグローバル変数と呼び、
使える範囲はグローバルスコープ内とローカルスコープ内の両方

## ローカルスコープ
・・・関数の内側で変数を定義すること
範囲はそれぞれの関数内のみで、その中で定義した変数はローカル変数と呼び、
使える範囲はローカル変数を定義した関数内のみ

## スコープを跨いだ変数の参照と変更
・・・ローカルスコープからグローバル変数の参照はできるが変更はできず、
一方グローバルスコープからローカル変数の参照・変更はどちらもできない。

## 引数

def 関数名(仮引数):
    処理名

関数名(実引数)

## for文のスコープ
・・・for文の外で定義した変数はfor文の中で使用でき、中で定義した変数はfor文の外で使用できる